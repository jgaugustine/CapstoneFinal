{
  "name": "capstone",
  "private": true,
  "workspaces": [
    "CapstoneHub"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=CapstoneHub",
    "build": "node scripts/build-all.mjs",
    "preview": "npm run preview --workspace=CapstoneHub",
    "dev:exposure": "PORT=3001 npm run dev --workspace=exposure-lab",
    "dev:bit-depth": "PORT=3002 npm run dev --workspace=bit-depth-visualizer",
    "dev:demosaic": "PORT=3003 npm run dev --workspace=demosaic-lab",
    "dev:image": "PORT=3004 npm run dev --workspace=image-lab",
    "dev:photon": "PORT=3005 npm run dev --workspace=photon-simulation"
  }
}
